# Dependencies
node_modules/
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Environment (contains secrets — NEVER commit)
.env
.env.local
.env.production
.env.prod
deploy/lighthouse/.env.prod

# SSH keys / certificates (NEVER commit)
*.pem
*.key
*.crt

# Generated output
output/
rendering/output/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build artifacts
dist/
build/
*.egg-info/

# Test artifacts
.pytest_cache/
htmlcov/
.coverage

# Logs
*.log

# Rendering
rendering/node_modules/
rendering/.vite/

# Temporary files (screenshots, images, scratch)
*.webp
*.png
*.jpg
*.jpeg
*.gif
!assets/**/*.png
!assets/**/*.jpg
!assets/**/*.webp
!public/**/*.png
!public/**/*.jpg
!public/**/*.webp
!web/public/**/*.png
!web/public/**/*.jpg
!web/public/**/*.webp
!rendering/public/**/*.png
!docs/guide/UI_design_info/*.png
!docs/guide/UI_design_info/*.jpg
!docs/guide/UI_design_info/*.jpeg

# One-off scripts (not part of the project)
# 只忽略 scripts/ 下的一次性 e2e / provider 探查脚本
# (旧规则 `test_*.py` 是全局,误伤了 tests/ 下的正式测试)
scripts/test_*.py
security_self_check.py

# Claude / AI artifacts
.claude/
.kiro/
.hermes/
.sisyphus/
.playwright-mcp/
.omc/

# Archive / drafts (keep locally, not in git)
tmp/
drafts/
archive/

# Portfolio index — 由 portfolio_hook 在 pipeline.completed 时自动 rebuild,
# 自动重新生成会污染 git diff。定位为本地缓存,不进版本控制。
# 跨机器看作品集:rsync output/ 后跑 `make portfolio` 重建本地 index。
assets/portfolio/index.json
*.log

# Backup files
*.bak
*.backup
*~

# Large binaries
*.mp4
*.mp3
*.wav
*.mov
*.avi
!output/**/*.mp4
!output/**/*.mp3

# Next.js build cache
.next/
.next.old/

# Baidu Cloud Sync temp metadata
*.baiduyun.uploading.cfg
.*.baiduyun.uploading.cfg
