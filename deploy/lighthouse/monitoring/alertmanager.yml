route:
  receiver: local-fixture
  group_by: [alertname]
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 4h
receivers:
  - name: local-fixture
    webhook_configs:
      - url: http://alert-receiver:8080/alerts
        send_resolved: true
