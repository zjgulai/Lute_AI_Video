# Core pipeline
langgraph>=0.2.0
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-anthropic>=0.3.0
pydantic>=2.0
python-dotenv>=1.0
httpx>=0.27
structlog>=24.0
pyyaml>=6.0
prometheus-client>=0.20

# LangGraph Postgres checkpoint persistence (P0-E)
# 生产环境 /pipeline/* 通过 PostgresSaver 跨重启恢复 checkpoint;
# 没有这两个包时 src/graph/pipeline.py:251 会报 ModuleNotFoundError 而 fail-fast。
psycopg[binary]>=3.2
langgraph-checkpoint-postgres>=3.0

# Backend API (optional: skip if no fastapi)
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9

# Database (optional: skip if using mock only)
supabase>=2.0
asyncpg>=0.29
alembic>=1.18.4  # 生产部署需要 alembic upgrade head 执行数据库迁移

# Admin panel auth (Phase 1)
bcrypt>=4.2

# S3 Influencer Remix — KOL video download + transcription
# yt-dlp: shell out via CLI (video_downloader._real_download)
# faster-whisper: Python binding, ~200MB vs openai-whisper+torch ~1.8GB
# (_real_transcribe uses faster_whisper.WhisperModel when available)
yt-dlp>=2025.9.5
faster-whisper>=1.0

# Quality module — CLIP text-image alignment (D15 Option CLIP-only)
# transformers + torch + Pillow enable src/quality/clip_alignment.py to score
# keyframe/storyboard alignment. ~600MB image growth (torch CPU wheels).
# Decision: keep CPU-only to stay under 4GB image budget. GPU users override
# via PIP_INDEX_URL + cuda-aware torch in deploy/lighthouse/.env.prod.
transformers>=4.40
torch>=2.0
Pillow>=10.0

# Development
pytest>=8.0
pytest-asyncio>=0.23
pytest-mock>=3.12
pytest-timeout>=2.3
pytest-cov>=4.1
ruff>=0.3
